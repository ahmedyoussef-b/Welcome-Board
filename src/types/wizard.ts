// This file is now obsolete. All types have been moved to src/types/index.ts
// for better organization and to avoid circular dependencies.
// It is kept temporarily to prevent breaking existing imports, but should be removed
// in a future cleanup.

export type {
    SchoolData,
    WizardData,
} from './index';
